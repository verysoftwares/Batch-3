-- file linking.
    require 'load'
    require 'alias'
    require 'utility'
    require 'update'
    require 'draw'

t = 0
-- runtime.
    print(fmt('made by verysoftwares with LÖVE %d.%d', love.getVersion()))